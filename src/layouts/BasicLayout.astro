---
import Footer from "../components/Footer.astro";
import Head from "../components/Head.astro";

const { title, marqueetext } = Astro.props;

let links = [
	{title: 'Home', url: '/'},
	{title: 'Projects', url: '/projects'}
]
---

<html lang="en-US">
	<Head title={title} />
	<body class="bg-white dark:bg-neutral-900">
		{marqueetext && <marquee class="bg-purple-600 dark:bg-purple-900 text-white font-mono">
			{marqueetext}
		</marquee>}
		<div class="flex items-center w-full flex-col gap-2 p-4 md:p-8 text-black dark:text-white">
			<div class="w-full max-w-xl">
				<slot />
				<div class="mt-8">
					<Footer links={links}/>
				</div>
			</div>
		</div>
	</body>
</html>
