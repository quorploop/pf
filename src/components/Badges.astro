---
const { badges } = Astro.props;
---

<div class="flex gap-2 py-1 font-mono text-s">
    {badges.map((badge) => {
        return <span class="px-2 text-white bg-primary rounded-full">{badge}</span>
    })}
</div>